<h1>Users　  index</h1>

<ul class="users">
  <% @users.each do |user| %>
    <span>image</span>
    <span>name</span>
    
    <div class="item"><%= attachment_image_tag user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg" %></div>
    <div class="item"><%= link_to(user.name, "/users/#{user.id}") %><%= user.name %></div>
    
  <% end %>
</ul>